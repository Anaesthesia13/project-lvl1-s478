#!/usr/bin/env node
import { knowName } from '..';
knowName ();

const x = (s, f) => Math.floor(Math.random() * (f - s));
const y = (s, f) => Math.floor(Math.random() * (f - s));
const oper = ['+', '-', '*'];
const sign = Math.random()*3;
const z = oper[sign];

const ask = console.log ('Question: ${x} ${z} ${y}');
const calcRandom = (x,y,z) => (x+z+y);
