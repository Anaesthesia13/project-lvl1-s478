#!/usr/bin/env node
import { knowName } from '..';
console.log ('Welcome to the Brain Games!');
knowName ();

export const SumOfRandom = (f,s) => {
const x = {
  return Math.random() * (f - s);
};
const y = {
  return Math.random() * (f - s);
};
const sign = [+, -, *];
const s = Math.floor(Math.random()*sign.length);
}
